apes = ["Pongo pygmaeus", "Pan njfifkso", "Gor noseque"]

for ape in apes:
    print (f"{ape} is an ape. Its name starts with {ape[0]}") # en este caso se asignan las variables en el print
    print (f"Its name has {len(ape)}")  


    #name_length = len(ape)
    #first_letter = ape[0]
    #print (f"{ape} is an ape. Its name starts with {first_letter}") #variable dentro de la llave. tambien se pueden operaciones
    #print (f"Its name has {name_length}")  

    # print (ape + " is an ape. Its name starts with" + first_letter)
    # print ("Its name has" + str (name_length))  Funcion str se esta concatenando un string con un int